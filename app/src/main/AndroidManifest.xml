<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codrata.concisessc_106">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NotesActivity"
            android:label="@string/title_activity_notes"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ActivationActivity"
            android:label="Login"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ActivatedApp.Main2Activity"
            android:label="Concise SSC 106"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ActivatedApp.MainActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".MovingFloatingActionButton" />
        <activity android:name=".ActivationRegistration" />
        <activity android:name=".About"></activity>
    </application>

</manifest>